{
  "name": "Notify Employee - Integration with Notification Service",
  "description": "Verifies that the /attendance/notify endpoint sends a notification via the notification service and logs the action.",
  "test_type": "integration",
  "input": "POST to /attendance/notify with valid employee_id and message, and valid Authorization header.",
  "output": "JSON response indicating notification was sent.",
  "success_criteria": [
    "HTTP status is 200",
    "response['success'] is True",
    "response['status'] == 'sent'",
    "response['message'] contains 'Notification sent to employee'"
  ],
  "error_scenarios": [
    "Notification service API failure",
    "Invalid employee_id",
    "Authentication failure"
  ]
}