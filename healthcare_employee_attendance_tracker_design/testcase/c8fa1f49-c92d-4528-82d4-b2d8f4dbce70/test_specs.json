{
  "name": "Validate Shift Assignment - Functional Success",
  "description": "Checks that /attendance/validate-shift endpoint confirms valid shift assignment for an employee.",
  "test_type": "functional",
  "input": "POST to /attendance/validate-shift with valid employee_id and shift_id, and valid Authorization header.",
  "output": "JSON response indicating shift assignment is valid.",
  "success_criteria": [
    "HTTP status is 200",
    "response['success'] is True",
    "response['status'] == 'valid'",
    "response['message'] == 'Shift assignment is valid.'"
  ],
  "error_scenarios": [
    "Invalid shift assignment",
    "Authentication failure",
    "Malformed input"
  ]
}